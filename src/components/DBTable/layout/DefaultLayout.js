import React from "react";

/**
 * default layout is non-layout
 * @param children {JSX.Element}
 * @returns {JSX.Element}
 */
export default function ({children}) {
  return (
    <>
      {children}
    </>
  )
}
